language: node_js
node_js:
  - lts/*
script:
  - ./deploy.sh
before_install:
  - gem install bundler
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: docs/.vuepress/dist
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master